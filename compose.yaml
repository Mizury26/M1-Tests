services:

  postgres:
    image: postgres:latest
    container_name: postgres_test
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: book_db
    ports:
      - "5432:5432"
    networks:
      - test-default

networks:

  test-default:
    driver: bridge
